(function() {
  var buttons = document.querySelectorAll("button");

  function clickMe(event){
    console.log('clicked!');
  }

  if (buttons){
    for(let i = 0; i < buttons.length; i++){
      // var currentButton = buttons[i];
      // var temp = document.querySelector("")
      // currentButton.addEventListener('click', clickMe);
      
    }
  }




  users();
  comments();
  sessions();
  posts();
})();
